# =============================================================================
# SECRET SANTA AGENT - OPENAI CONFIGURATION
# =============================================================================

# OpenAI API Configuration
LLM_PROVIDER=openai
OPENAI_API_KEY="your_openai_api_key"

# Cognee uses OpenAI for both LLM and embeddings
LLM_API_KEY=${OPENAI_API_KEY}
LLM_MODEL=gpt-4.1-mini # I'm using the cheapest model available
# so It costed less than 0.2$ for the entire development, so go ahead to input your own key. SInce my mac is suprt slow, I was not able to use a local llm.
EMBEDDING_MODEL=text-embedding-ada-002
EMBEDDING_DIMENSIONS=1536

# Optional settings
GRAPHISTRY_USERNAME=
GRAPHISTRY_PASSWORD=

DISABLE_TELEMETRY=true

